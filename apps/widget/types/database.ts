// NOTE:
// `supabase/types.ts` is the canonical, generated type source (see `npm run gen:types`
// and `.github/workflows/supabase-types.yml`).
//
// This file exists as a stable import path for the rest of the codebase.

export type {
  Json,
  Database,
  Tables,
  TablesInsert,
  TablesUpdate,
  Enums,
  CompositeTypes,
} from "../supabase/types";

export { Constants } from "../supabase/types";
